# Wildberries 卖家后台商品全量数据拉取方案

> 项目路径：`C:\Users\yuanbo\PycharmProjects\WelcomeScreen`
> 目标：全量导出 WB 卖家后台（seller.wildberries.ru）"所有商品"列表数据（10 万级以上），并支持定时无人值守运行。

---

## 1. 数据源

| 项 | 值 |
|---|---|
| 页面 | `https://seller.wildberries.ru/card-main/all-goods` |
| 接口 | `POST https://seller-content.wildberries.ru/ns/viewer/content-card/viewer/tableListv6` |
| 鉴权 | 请求头 `AuthorizeV3`、`Wb-Seller-Lk`、`root-version`（token 会过期，每次运行从页面新捕获） |
| 分页 | 请求体 `cursor` 对象：`{"next":true,"n":20,"value":"<更新时间戳>","nmID":<商品ID>}`，响应 `data.cursor` 原样回填即翻下一页 |
| 响应 | `data.cards[]`，每页约 20 条，商品主键为 `nmID` |

## 2. 方案演进（踩坑记录）

1. **Selenium + 独立空白资料目录** → 浏览器无登录态 cookie。
2. **直接指向日常 Chrome 的 User Data 目录** → 失败。Chrome 136+ 安全限制：对默认资料目录静默忽略 `--remote-debugging-port`，`DevToolsActivePort` 永远不生成，chromedriver 连不上（报错 `SessionNotCreatedException`）。
3. **复制日常资料到项目内 `chrome_profile/`** → 成功。cookie 用 Windows DPAPI 加密、绑定本机当前用户，整目录复制（含 `Local State` 密钥文件）后在同一台机器同一用户下可正常解密，登录态直接可用，且可与日常 Chrome 同时运行。
4. **滚动页面触发无限加载**（script.py）→ 小数据量可行；10 万级数据渲染导致浏览器崩溃，弃用。
5. **最终方案：请求重放直连**（fetch_all.py）→ 捕获一次真实请求后，在页面上下文用 `fetch` 循环调接口翻页，**零渲染**，浏览器只挂一个静止页面，稳定拉取 24 万条以上无压力。

## 3. 最终架构

```
fetch_all.py
 ├─ 启动 Chrome（chrome_profile 登录态）打开商品页
 ├─ 从 performance 日志捕获首条 tableListv6 真实请求
 │    （跳过 OPTIONS 预检；取 method/headers/postData）
 ├─ 循环：页面内 fetch 重放请求 → 解析 data.cards / data.cursor
 │    ├─ 游标回填请求体，翻下一页
 │    ├─ 429 限流 → 暂停 30s 重试（不计失败）
 │    ├─ 调用异常 → 重试，连续 3 次失败保存断点退出
 │    └─ 每 100 次调用写一个分片文件 + 更新 state.json
 └─ 终止：cursor 为空 / 不变 / 空页 → finished=true，写 summary.json
```

### 关键参数（fetch_all.py 顶部常量）

| 常量 | 值 | 说明 |
|---|---|---|
| `SHARD_SIZE` | 100 | 每 100 次调用写一个分片文件 |
| `CALL_INTERVAL` | 2.5s | 调用间隔；实测 <1s 会在 ~36 次后触发 429 |
| `RATE_LIMIT_PAUSE` | 30s | 遇 429 后的长暂停 |
| `CAPTURE_TIMEOUT` | 120s | 等首条请求的超时 |

### 输出目录结构

```
tableListv6_<启动时间戳>/
├── tableListv6_shard_001_<ts>.json   # 第 1~100 次响应原文（数组）
├── tableListv6_shard_002_<ts>.json
├── ...
├── state.json                        # 断点状态：下一分片号/调用数/商品数/最后游标/finished
└── summary.json                      # 汇总：总调用数/总条数/finished/首条请求样本
```

### 断点续传

```powershell
python fetch_all.py --resume
```

- 自动定位最近一次未完成运行目录，恢复计数、分片编号与游标，续写同一目录；
- 优先读 `state.json`；对崩溃退出的旧目录（无 state.json），自动从**最后一个分片的最后一条响应**里恢复 `data.cursor`；
- 鉴权头每次续传都重新捕获，不存在 token 过期问题。

### 数据统计

`_count_nmid.py`：递归统计分片中 `nmID` 总数/去重数（用前把 `FILE` 指向目标分片文件；统计整个目录需遍历所有 shard）。

## 4. 定时任务（方案 A：任务计划程序 + bat 胶水层）

### 三层结构

```
Windows 任务计划程序 (schtasks)   ← 触发：每天定点；自带执行历史/失败重跑/错过补跑
        │
run_fetch.bat                     ← 胶水：搭环境、记日志、判断结果、自动 --resume 重试、防重叠
        │
fetch_all.py                      ← 干活：拉数据（自带断点续传）
```

### bat 的四项职责

1. **搭环境**：cd 项目目录、指定 venv python、`PYTHONIOENCODING=utf-8`（否则中文日志乱码）；
2. **记日志**：stdout/stderr 重定向到 `logs\fetch_<日期>.log`；
3. **自动续传**：跑完检查最新目录 `state.json` 的 `finished`，为 false 则等 60s 后 `--resume` 重试，最多 3 轮；
4. **防重叠**：锁文件避免上一轮未结束又触发下一轮（两个进程会抢同一个 chrome_profile）。

### 注册命令

```powershell
schtasks /Create /TN "WB_FetchAll" /TR "C:\Users\yuanbo\PycharmProjects\WelcomeScreen\run_fetch.bat" /SC DAILY /ST 03:00 /F
```

bat 模板见 skill 目录 `.qoder/skills/wb-seller-data-fetch/scripts/run_fetch.bat`，复制到项目根目录即可。

### 必须注意

- 任务属性选 **"只在用户登录时运行"**：脚本要启动带界面的 Chrome，无人登录的会话 0 起不来 GUI；
- 到点时电脑需开机且已登录；
- **不要试图把浏览器部分搬进 Docker/Linux**：chrome_profile 的 cookie 是 Windows DPAPI 加密（绑定本机用户），Linux 容器解不开，容器内重新登录会撞验证码。

## 5. 常驻宿主（方案 B：Flask + APScheduler 巡检自愈）

> 方案 A（bat）用“定时触发 + 重试循环”兜底；方案 B 把 `fetch_all` 装进一个**常驻 Flask 宿主进程**（Windows 本机，**非 Docker**——DPAPI cookie / Chrome 136+ 约束不变），worker 常驻后台线程持续拉取，cron 守护线程定时巡检、挂了自动续传重启。

### 三层结构

```
app.py  (waitress 常驻, 127.0.0.1:8080)
 ├─ Flask 路由：/health /status /start /stop /resume（?store=<id> 选店，缺省 DEFAULT_STORE）
 ├─ supervisors = {store_id: FetchSupervisor}   ← 每店一个，各自独立 worker/锁/profile/进度
 │    每个 FetchSupervisor (supervisor.py)：
 │    ├─ worker 线程 ── fetch_all.run_fetch(resume, stop_event, progress, store)
 │    │      · 线程内独占 create_driver(profile_dir=本店)→用→quit（Selenium 线程亲和）
 │    │      · 循环内查 stop_event 优雅停；持续写 progress["snap"] 原子快照
 │    │      · PID 单实例锁 <data_dir>/fetch_all.lock + pre_launch_cleanup(本店 profile) 清孤儿
 │    └─ APScheduler BackgroundScheduler ── cron(*/2) inspect() 巡检
 │           · worker 挂了 & 非用户停 & 未拉完 → 自动 --resume 重启（restart_count++）
 └─ config.py：HOST / PORT / CRON_EXPR / AUTO_START / STORES（多店注册表）
```

### 线程与锁的正确性（关键，勿踩坑）

| 机制 | 层级 | 作用 |
|---|---|---|
| `threading.Lock`（非阻塞获取） | 进程内 | 杜绝同进程双 worker（PID 锁挡不住同进程多线程）；`/start` 与巡检抢锁时后来者跳过 |
| `<data_dir>/fetch_all.lock`（PID 级，每店一个） | 跨进程 | 宿主 worker 与手动 CLI 防撞车；同进程重入放行（`old_pid==getpid()`），他进程存活则 `[LOCK]` 挡下；每店独立锁，A 店不挡 B 店 |
| `stop_event` | 进程内 | `/stop` 置位 → worker 最多滞后一个调用周期退出并存断点；同时置 `user_stopped`，巡检不再自动重启 |
| `progress["snap"]` 原子替换 | 进程内 | worker 写、`/status` 读，无需加锁（整体替换 dict 引用） |

- **driver 线程亲和**：Selenium 命令必须在创建 driver 的同一线程发出，worker 全程独占，绝不跨线程。
- `inspect()` 只在 scheduler 线程读 `is_alive()`/progress，需重启才 spawn，快进快出；`max_instances=1, coalesce=True`。
- Flask **不能开 debug/reloader**（模块双加载 → 双 scheduler/双 worker）；`waitress.serve(app,...)` 直接传对象不走导入字符串。

### 巡检终态（不自动重启的情形）

`finished=true`（全部拉完）、`error=no_resume_state`（历次均完成、无可续传）、`error=locked`（他进程持锁）、`error=store_mismatch` / `error=store_verify_failed` / `error=store_not_available`（抓取前自动选店/校验不通过，需人工确认店铺）、`user_stopped`（用户 `/stop`）。其余（崩溃 / capture_timeout / 服务端错误退出）均自动 `--resume` 重启。

### 部署（登录自启，取代 bat 重试循环）

```powershell
schtasks /Create /TN "WB_FetchHost" /TR "C:\Users\yuanbo\PycharmProjects\WelcomeScreen\.venv\Scripts\pythonw.exe C:\Users\yuanbo\PycharmProjects\WelcomeScreen\app.py" /SC ONLOGON /F
```

- 任务属性 **“只在用户登录时运行”**（Chrome GUI 需交互式会话，会话 0 起不来）；
- `AUTO_START=True` → 宿主一起来就 `--resume` 从 `state.json` 无缝续上，不丢数据；
- 宿主被强杀会留孤儿 chrome，下次启动 `pre_launch_cleanup(profile_dir)` 自动清理（**只杀命令行含本店 profile 路径**（边界匹配）的 chrome + 其父 chromedriver，不碰日常浏览器与兄弟店）。

### CLI 兼容

`fetch_all.py` 命令行仍独立可用：`python fetch_all.py [--resume] [--store <id>]`（`main()` 是 `run_fetch()` 的薄壳，`--store` 缺省 `config.DEFAULT_STORE`）。

### 多店铺并行（多账号）

一台机抓多个 WB 店铺，逻辑平移、按店隔离。`config.STORES` 每店一条：

| 字段 | 含义 |
|---|---|
| `id` | 店铺标识；用作 `?store=` 参数 / 锁文件名 / 日志前缀 / OSS 段（缺省） |
| `profile_dir` | 该店独立 Chrome user-data-dir（cookie 隔离，同页不同账号不撞登录态） |
| `data_dir` | 该店运行目录（`tableListv6_*` + `fetch_all.lock`）；现有店用 `.` 保断点零迁移 |
| `oss_segment` | OSS prefix 追加段 → `wildberries/wbRatingData/<段>/json/<上传当天BJT日期>/文件名`（无时分秒层，日期按上传实时算） |
| `auto_start` | 缺省=全局 `AUTO_START`；新店登录前置 `False` 防未登录空转重启 |

- 每店一个 `FetchSupervisor`（独立 worker 线程 / `_worker_lock` / stop_event / progress / cron 巡检），三店并发；HTTP 接口用 `?store=<id>`（缺省 DEFAULT_STORE，`all` 遍历全部）。
- **新店首次登录**：DPAPI cookie 无法自动登录，跑 `python login_store.py <id>` 弹浏览器人工登录一次，cookie 落到该店 profile，之后抓取自动复用。
- **清理不误伤**：`pre_launch_cleanup(profile_dir)` 按 profile 路径边界匹配 + 只杀关联父 chromedriver，不再 blanket-kill 全部 chromedriver（否则并行时会杀兄弟店驱动）；profile 命名勿互为前缀（用 `profiles/storeN`）。
- **并发资源**：3 店 = 3 chrome 常驻，内存/CPU 约 3 倍；各店独立账号 429 互不影响。

### 抓取前自动选店（按 config.json 切到目标店，防爬错店）

多店可能共用同一登录子账号，且不同店在切换下拉框里**显示名可能完全相同**（实测 store1/store2 名字一模一样），靠名字无法区分。`fetch_all.py` 在 `driver.get(PAGE_URL)` 之后、**捕获 tableListv6 请求之前**先自动把下拉框切到 `config.json` 指定的店——切换会改供应商 cookie/token 上下文，必须在捕获前完成，否则重放的是旧店请求：

```
config.json（项目根）  {"store1": 250132124, "store2": 250149024}   ← 每店期望的 WB 数字卖家 ID
        │  config.get_seller_id(store_id)
        ▼
ensure_selected_store(driver, expected_id)   ← 在捕获请求之前
 ├─ JS .click() 点开 chip [data-testid=desktop-profile-select-button-chips-component]（普通 click 展不开）
 ├─ 轮询等 input[type=radio][name=supplier]，遍历每个 <li> 取 [data-name="Text"]，从 "ID <数字> • 税号 <数字>" 解析数字 ID
 ├─ 目标行已 checked → already（不动）；否则 JS 点击目标行 radio 切换 → switched
 └─ 下拉框无此店 → not_found；点击失败 → click_failed；展不开 → open_failed
        ▼
switched 时：sleep(3) 等落地 → get_log('performance') 丢弃旧店请求 → driver.get(PAGE_URL) 干净重载
        ▼
wait_first_request 捕获【目标店】的 tableListv6 → verify_selected_store 只读复核 checked==目标
        ▼
结果码：not_found→store_not_available；复核不符→store_mismatch；展不开/读不到→store_verify_failed（均 quit + 终态不重启）
```

- **只用稳定选择器**（`data-testid` / `type=radio name=supplier` / `data-name=Text` / "ID <数字>" 正则），刻意避开 WB 构建期哈希 class（`text_Text__CfKFk`、`suppliers-list_...__9lMrO` 等，改版即失效）。
- **不读 cookie/JWT**：子账号共享、且要求纯 DOM 方案；虽然 `auth/token` 的 JWT `Z-Soid` 也含数字 ID，但不采用。
- 未配置该店 ID→跳过选店并提示补上（向后兼容）；store1 已停在目标店时走 already 分支，行为等同旧版仅校验。

## 6. 运维手册（常见故障）

| 现象 | 原因 | 处理 |
|---|---|---|
| 启动报 `DevToolsActivePort file doesn't exist` | chrome_profile 被残留 Chrome 进程锁住 | `Get-Process chrome,chromedriver` 检查并结束后重跑 |
| 接口返回 401 | 登录态失效 | 手动开一次 `script.py`（或 Chrome 挂该 profile）重新登录 WB，再跑 |
| 接口返回 429 | 触发限流 | 脚本自动暂停 30s 重试，无需干预；频繁出现可加大 `CALL_INTERVAL` |
| 脚本超时崩溃退出 | fetch 挂起/浏览器失去响应 | `python fetch_all.py --resume` 续传 |
| 数据疑似重复 | 续传边界重叠一页 | 按 `nmID` 全局去重（`_count_nmid.py` 可验证） |
| 定时任务没跑 | 电脑未开机/未登录，或锁文件残留 | 查任务计划程序历史；删 `%TEMP%\wb_fetch_all.lock` |
| Flask 宿主 worker 反复重启 | 多为登录态失效致 capture_timeout | 看 `/status` 的 `last_error`；`script.py` 重登后 `/resume` |
| Flask `/status` 显示 user_stopped 不重启 | `/stop` 后巡检尊重用户意图 | `/resume` 或 `/start` 重启 |
| 多店：新店 worker 反复 capture_timeout | 该店 profile 未登录 | `login_store.py <id>` 登录；未登录前 `auto_start=False` |
| worker 反复重启，`/status.last_error` 含 `UnicodeEncodeError: 'gbk'` | 日志 print 里有非 GBK 字符（emoji ✓/✅/❌/🔁），Windows GBK stdout 编码失败崩溃；`run_fetch` 通用异常只记 `status=error` 不设终态码→巡检误判可重启→反复重启 | 已在 `fetch_all.py` 顶部 `sys.stdout/stderr.reconfigure(encoding=utf-8, errors=replace, line_buffering=True)` 兜底；print 内禁用 emoji（改 `[OK]`/`[X]`/`*`），中文本身 GBK 可编码不受影响 |
| 宿主 `host_new.log` 长期 0 字节但 `/status` 正常在爬 | `Start-Process -WindowStyle Hidden -RedirectStandardOutput` 不落盘（PowerShell 已知坑：Hidden 窗口 + 重定向不捕获） | 改 `-NoNewWindow`（重定向才生效），或 `.venv\Scripts\python.exe -u app.py > host_new.log 2>&1`；`-u` + 代码 `line_buffering=True` 保证实时可见 |
| 多店：怕 cleanup 误杀兄弟店 chrome | profile 路径边界匹配 + 只杀关联父 chromedriver | profile 命名勿互为前缀（用 `profiles/storeN`） |
| 多店：`/status` 的 `last_error=store_mismatch` | 自动切换后复核仍与 `config.json` 期望 ID 不符 | 人工在该店 profile 里手动切到目标店/核对登录账号后 `/resume`；终态不自动重启 |
| 多店：`last_error=store_not_available` | 下拉框里没有 `config.json` 期望的店 ID | 该子账号可能无此店权限，核对账号与店铺 ID；终态不自动重启 |
| 多店：`last_error=store_verify_failed` | 下拉框没展开/没读到/点击切换失败（改版/渲染慢） | 看日志 `[SELECT]`/`[VERIFY]` 行，必要时更新稳定选择器；终态不自动重启 |

## 7. 已知限制

- 登录态过期需要人工重登（验证码无法自动化）；
- 强绑定这台 Windows 机器（DPAPI）；
- 拉取速率受限流约束：约 2.5~3s/页 × 20 条/页 ≈ **2 万条/小时**；
- 并行化（多线程分段拉链）理论可行但需验证服务端是否接受自造游标，暂未实施；
- 多店并行（§5）是“每店各拉各的账号链”，非单账号分段；每店需各自人工登录一次，3 店并发资源约 3 倍。

## 8. 文件清单

| 文件 | 作用 | 状态 |
|---|---|---|
| `fetch_all.py` | 主脚本：直连拉取+分片+断点续传；`run_fetch(resume,stop_event,progress,store)` 供宿主调用，每店 PID 锁 + `pre_launch_cleanup(profile_dir)`；采集前 `ensure_selected_store` 按 config.json 自动选店、切换后 `verify_selected_store` 复核 | ✅ 在用 |
| `script.py` | 滚动方案（备用）；`fetch_all.py` 依赖其 `create_driver(headless, profile_dir)` | ✅ 在用 |
| `wb_to_oss.py` | 分片转 CSV + 上传 OSS（`oss_target→bucket/prefix/err` 3 元组，prefix 可加店铺段，日期目录=上传当天 BJT、无时分秒层） | ✅ 在用 |
| `config.py` | Flask 宿主配置：HOST/PORT/CRON_EXPR/AUTO_START + `STORES` 多店注册表 + `get_seller_id()` | ✅ 在用 |
| `config.json` | 每店期望的 WB 数字卖家 ID（`{"store1":250132124,...}`），抓取前店铺校验比对用 | ✅ 在用 |
| `supervisor.py` | `FetchSupervisor(store)`：每店一 worker 线程 + `threading.Lock` + APScheduler cron 巡检自愈 | ✅ 在用 |
| `app.py` | Flask 宿主：多店 supervisor + 路由 `/health /status /start /stop /resume`（`?store=`）+ `waitress.serve` | ✅ 在用 |
| `login_store.py` | 新店一次性人工登录，把 cookie 落到该店独立 profile | ✅ 在用 |
| `requirements.txt` | flask/waitress/apscheduler/selenium/oss2 | ✅ 在用 |
| `_count_nmid.py` | nmID 统计工具 | ✅ 在用 |
| `chromedriver.exe` / `chrome_profile/` | 驱动与登录态资料 | ✅ 核心依赖 |
| `run_fetch.bat` | 定时任务胶水层（旧方案 A，模板在 skill 内） | ⬜ 备用 |
